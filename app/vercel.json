{
  "framework": "nextjs",
  "regions": ["gru1"],
  "buildCommand": "npx prisma generate && npx prisma migrate deploy && next build",
  "installCommand": "npm install",
  "crons": [
    {
      "path": "/api/cron/daily",
      "schedule": "0 12 * * *"
    }
  ]
}
